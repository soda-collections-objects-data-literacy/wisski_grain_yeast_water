name: 'WissKI Grain'
description: 'WissKI base environment with mandatory modules.'
type: 'Site'

install:
  - inline_entity_form
  - wisski

config:
  import:
    inline_entity_form: "*"
    wisski: "*"
